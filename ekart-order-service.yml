server:
  port: 8180

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kodewala_practice_db
    username: root
    password: Manoj@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
